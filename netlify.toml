[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Remove the incorrect redirects that were causing the issue
# Next.js handles its own routing
